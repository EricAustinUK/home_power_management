# Loaded by /etc/init.d/S11modules (reads modules-load.d, NOT /etc/modules)
# feature_disable=0x2000 clears BRCMF_FEAT_FWSUP (bit 13): forces host-side
# 4-way handshake — firmware supplicant hangs against the Plusnet/Arcadyan AP
brcmfmac feature_disable=0x2000
